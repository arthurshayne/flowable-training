spring:
  datasource:
    url: jdbc:h2:mem:testflow;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  main:
    banner-mode: "off"


flowable:
  dmn:
    enabled: false
  cmmn:
    enabled: false
  app:
    enabled: false
  form:
    enabled: false